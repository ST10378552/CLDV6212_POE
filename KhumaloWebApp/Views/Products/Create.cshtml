@model KhumaloWebApp.Models.Order

@{
    ViewData["Title"] = "Order Form";
}

<h2 class="text-center mb-4" style="font-weight: bold; color: #333;">Order Form</h2>

<div class="row justify-content-center">
    <div class="col-md-8">
        <form asp-action="CreateOrder" style="background-color: #FFDAB9; padding: 40px; border-radius: 10px; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);">
            <div class="form-group mb-4">
                <label asp-for="ProductId" class="control-label" style="font-weight: bold;">Product</label>
                <select asp-for="ProductId" class="form-control">
                    @for (int i = 1; i <= 20; i++)
                    {
                        <option value="@i">Product @i</option>
                    }
                </select>
                <span asp-validation-for="ProductId" class="text-danger"></span>
            </div>
            <div class="form-group mb-4">
                <label asp-for="Quantity" class="control-label" style="font-weight: bold;">Quantity</label>
                <select asp-for="Quantity" class="form-control">
                    @for (int i = 1; i <= 10; i++)
                    {
                        <option value="@i">@i</option>
                    }
                </select>
                <span asp-validation-for="Quantity" class="text-danger"></span>
            </div>
            <div class="form-group mb-4">
                <label asp-for="TotalPrice" class="control-label" style="font-weight: bold;">Total Price</label>
                <input asp-for="TotalPrice" class="form-control" placeholder="Total Price" readonly />
                <span asp-validation-for="TotalPrice" class="text-danger"></span>
            </div>
            <div class="form-group mb-4">
                <label asp-for="OrderDate" class="control-label" style="font-weight: bold;">Order Date</label>
                <input asp-for="OrderDate" class="form-control" placeholder="Order Date" readonly />
                <span asp-validation-for="OrderDate" class="text-danger"></span>
            </div>
            <div class="form-group text-center" style="margin-top: 20px;">
                <button type="submit" class="btn btn-primary" style="padding: 10px 20px; font-weight: bold; font-size: 16px;">Save</button>
                <a asp-action="OrderHistory" class="btn btn-secondary" style="padding: 10px 20px; font-weight: bold; font-size: 16px; margin-left: 10px;">Back to Order History</a>
            </div>
        </form>
    </div>
</div>
